name: Publish Desktop Artifacts

on:
  push:
    branches:
      - main
    paths:
      - "apps/electron/package.json"
  workflow_dispatch:

permissions:
  contents: read

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  prepare:
    if: github.repository == 'FranciscoMoretti/chat-js'
    name: Prepare desktop artifact publish
    runs-on: ubuntu-22.04
    outputs:
      should_release: ${{ steps.versions.outputs.should_release }}
      tag: ${{ steps.versions.outputs.tag }}
      version: ${{ steps.versions.outputs.version }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - id: versions
        name: Determine release version
        shell: bash
        run: |
          VERSION=$(python3 - <<'PY'
          import json
          with open("apps/electron/package.json") as f:
              print(json.load(f)["version"])
          PY
          )

          SHOULD_RELEASE=true

          if [ "${{ github.event_name }}" = "push" ] && [ "${{ github.event.before }}" != "0000000000000000000000000000000000000000" ]; then
            PREVIOUS_VERSION=$(python3 - <<'PY'
          import json
          import subprocess
          import sys

          try:
              raw = subprocess.check_output(
                  ["git", "show", "${{ github.event.before }}:apps/electron/package.json"],
                  text=True,
                  stderr=subprocess.DEVNULL,
              )
          except subprocess.CalledProcessError:
              print("")
              sys.exit(0)

          print(json.loads(raw)["version"])
          PY
            )

            if [ -n "$PREVIOUS_VERSION" ] && [ "$PREVIOUS_VERSION" = "$VERSION" ]; then
              SHOULD_RELEASE=false
            fi
          fi

          {
            echo "version=$VERSION"
            echo "tag=@chat-js/electron@$VERSION"
            echo "should_release=$SHOULD_RELEASE"
          } >> "$GITHUB_OUTPUT"

  build:
    name: Build ${{ matrix.label }}
    needs: prepare
    if: github.repository == 'FranciscoMoretti/chat-js' && needs.prepare.outputs.should_release == 'true'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: macOS
            os: macos-latest
            platform: darwin
            arch: universal
          - label: Windows
            os: windows-latest
            platform: win32
            arch: x64
          - label: Linux
            os: ubuntu-22.04
            platform: linux
            arch: x64

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.3.1

      - uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-

      - run: bun install --frozen-lockfile

      - name: Install Linux packaging dependencies
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y fakeroot rpm

      - name: Build Forge distributables
        working-directory: apps/electron
        run: bun run make -- --platform=${{ matrix.platform }} --arch=${{ matrix.arch }}

      - name: Upload release assets
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: electron-release-${{ matrix.platform }}-${{ matrix.arch }}
          path: apps/electron/out/make/**/*
          if-no-files-found: error

  publish:
    name: Publish desktop artifacts
    permissions:
      contents: write
    needs: [prepare, build]
    if: github.repository == 'FranciscoMoretti/chat-js' && needs.prepare.outputs.should_release == 'true'
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Download release artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          path: /tmp/electron-release-assets

      - name: Create package release tag and GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ needs.prepare.outputs.tag }}
          RELEASE_VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          gh auth setup-git
          TAG_SHA=$(git rev-list -n 1 "refs/tags/$RELEASE_TAG" 2>/dev/null || true)
          if [ -n "$TAG_SHA" ] && [ "$TAG_SHA" != "$GITHUB_SHA" ]; then
            echo "$RELEASE_TAG already points to $TAG_SHA; refusing to publish assets from $GITHUB_SHA." >&2
            exit 1
          fi

          if ! git rev-parse "$RELEASE_TAG" >/dev/null 2>&1; then
            git tag "$RELEASE_TAG" "$GITHUB_SHA"
            git push origin "$RELEASE_TAG"
          fi

          if ! gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release create "$RELEASE_TAG" \
              --title "@chat-js/electron $RELEASE_VERSION" \
              --notes "Desktop artifact release for @chat-js/electron $RELEASE_VERSION." \
              --target "$GITHUB_SHA"
          fi

      - name: Upload release assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ needs.prepare.outputs.tag }}
        shell: bash
        run: |
          mapfile -t files < <(find /tmp/electron-release-assets -type f)
          if [ "${#files[@]}" -eq 0 ]; then
            echo "No release assets found."
            exit 1
          fi
          gh release upload "$RELEASE_TAG" "${files[@]}" --clobber
